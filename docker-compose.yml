version: "3.8"

services:
  bot:
    build: .
    container_name: telegram_bot
    env_file: .env
    volumes:
      - ./bookings.db:/app/bookings.db
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
